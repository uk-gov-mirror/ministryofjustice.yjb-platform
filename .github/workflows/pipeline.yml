name: Pipeline [test -> build -> deploy]

on:
  push:
    branches:
      - '**'
  workflow_dispatch:
    inputs:
      additional_docker_tag:
        description: Additional docker tag that can be used to specify stable or testing tags
        required: false
        default: ''
        type: string
      push:
        description: Push docker image to registry flag
        required: true
        default: false
        type: boolean
      publish_feature_branch:
        description: Push docker image to registry and deploy app even if we are on a feature branch
        required: false
        default: false
        type: boolean

permissions:
  contents: read
  packages: write
  id-token: write
  attestations: write

concurrency:
  # Only cancel in progress when on a branch - use SHA on main to ensure uniqueness.
  # This will allow multiple pipelines to run at the same time on main.
  # Note that two pipelines running within a few minutes of each other will cause issues with helm deploying to
  # a namespace, as the second one will not be able to get a lock as a deploy will be in progress.
  group: ${{ github.workflow }}-${{ github.ref == 'refs/heads/main' && github.sha || github.ref }}
  cancel-in-progress: ${{ github.ref != 'refs/heads/main' }}

jobs:
  # Determines whether the pipeline should publish the docker image.
  # Referenced by build and deploy jobs to avoid repeating the condition.
  check_publish:
    name: Check publish flag
    runs-on: ubuntu-latest
    outputs:
      enabled: ${{ (github.ref == 'refs/heads/main' && (inputs.push || inputs.push == null)) || inputs.publish_feature_branch }}
    steps:
      - name: Set publish flag
        run: echo "Publishing enabled ${{ (github.ref == 'refs/heads/main' && (inputs.push || inputs.push == null)) || inputs.publish_feature_branch }}"

  # main node build workflow
  node_build:
    name: node build
    uses: ministryofjustice/hmpps-github-actions/.github/workflows/node_build.yml@v2.22.2 # WORKFLOW_VERSION
    secrets: inherit
    with:
      artifact_path: |
        apps/frontend/dist/
        apps/frontend/assets/stylesheets/
        apps/api/dist/
#      install_command: 'npm run setup'

  # generic node unit tests - feel free to override with local tests if required
  node_unit_tests_frontend:
    name: unit tests - ui
    uses: ministryofjustice/hmpps-github-actions/.github/workflows/node_unit_tests.yml@v2.22.2 # WORKFLOW_VERSION
    needs: [node_build]
    secrets: inherit
    with:
      npm_script: 'test:fe:no-logs'
      download_path: 'apps/'
      test_results_artifact_name: 'npm_unit_test_frontend_artifacts'
      test_results_artifact_path: 'apps/frontend/coverage'

  # generic node unit tests - feel free to override with local tests if required
  node_unit_tests_api:
    name: unit tests - api
    uses: ministryofjustice/hmpps-github-actions/.github/workflows/node_unit_tests.yml@v2.22.2 # WORKFLOW_VERSION
    needs: [ node_build ]
    secrets: inherit
    with:
      npm_script: 'test:api'
      download_path: 'apps/'
      test_results_artifact_name: 'npm_unit_test_api_artifacts'
      test_results_artifact_path: 'apps/api/coverage'

  # TODO: remove this job until we have integration tests
  #  # generic node integration tests using wiremock - feel free to override with local tests if required
  #  node_integration_tests:
  #    name: node integration tests
  #    uses: ministryofjustice/hmpps-github-actions/.github/workflows/node_integration_tests.yml@v2 # WORKFLOW_VERSION
  #    needs: [node_build]
  #    secrets: inherit

  helm_lint_frontend:
    strategy:
      matrix:
        environments: ['dev', 'preprod', 'prod']
    name: helm lint - ui
    uses: ministryofjustice/hmpps-github-actions/.github/workflows/test_helm_lint.yml@v2 # WORKFLOW_VERSION
    secrets: inherit
    with:
      environment: ${{ matrix.environments }}

  helm_lint_api:
    strategy:
      matrix:
        environments: ['dev', 'preprod', 'prod']
    name: helm lint - api
    uses: ministryofjustice/hmpps-github-actions/.github/workflows/test_helm_lint.yml@v2 # WORKFLOW_VERSION
    secrets: inherit
    with:
      environment: ${{ matrix.environments }}
      chart_name: 'yjb-platform-api'
      helm_dir: 'helm_deploy_api'

  # TODO: This step might need duplication to handle multiple builds for monorepo
  build_frontend:
    name: build image - ui
    uses: ministryofjustice/hmpps-github-actions/.github/workflows/docker_build.yml@v2 # WORKFLOW_VERSION
    needs:
 #      - node_integration_tests
      - node_unit_tests_frontend
#      - node_build
      - check_publish
    with:
      docker_registry: 'ghcr.io'
      registry_org: 'ministryofjustice'
      additional_docker_tag: ${{ inputs.additional_docker_tag }}
      push: ${{ needs.check_publish.outputs.enabled == 'true' }}
      docker_multiplatform: false
      generate_sbom: ${{ needs.check_publish.outputs.enabled == 'true' }}
      attach_sbom_to_registry: ${{ needs.check_publish.outputs.enabled == 'true' }}
      sign_image: ${{ needs.check_publish.outputs.enabled == 'true' }}
      image_name: 'yjb-platform'
      file: 'apps/frontend/Dockerfile'

  build_api:
    name: build image - api
    uses: ministryofjustice/hmpps-github-actions/.github/workflows/docker_build.yml@v2 # WORKFLOW_VERSION
    needs:
      #      - node_integration_tests
      - node_unit_tests_api
#      - node_build
      - check_publish
    with:
      docker_registry: 'ghcr.io'
      registry_org: 'ministryofjustice'
      additional_docker_tag: ${{ inputs.additional_docker_tag }}
      push: ${{ needs.check_publish.outputs.enabled == 'true' }}
      docker_multiplatform: false
      generate_sbom: ${{ needs.check_publish.outputs.enabled == 'true' }}
      attach_sbom_to_registry: ${{ needs.check_publish.outputs.enabled == 'true' }}
      sign_image: ${{ needs.check_publish.outputs.enabled == 'true' }}
      image_name: 'yjb-platform-api'
      file: 'apps/api/Dockerfile'

  deploy_dev_frontend:
    name: deploy to dev - ui
    if: needs.check_publish.outputs.enabled == 'true'
    needs:
      - build_frontend
      - helm_lint_frontend
      - check_publish
    uses: ministryofjustice/hmpps-github-actions/.github/workflows/deploy_env.yml@v2 # WORKFLOW_VERSION
    secrets: inherit
    with:
      environment: 'dev'
      app_version: '${{ needs.build_frontend.outputs.app_version }}'
      #app_version: 'local-test-1.0.0' #local act override
      slack_notification: 'false' # if 'true', it needs a valid NONPROD_RELEASES_SLACK_CHANNEL variable to be set

  deploy_dev_api:
    name: deploy to dev - api
    if: needs.check_publish.outputs.enabled == 'true'
    needs:
      - build_api
      - helm_lint_api
      - check_publish
    uses: ministryofjustice/hmpps-github-actions/.github/workflows/deploy_env.yml@v2 # WORKFLOW_VERSION
    secrets: inherit
    with:
      environment: 'dev'
      app_version: '${{ needs.build_api.outputs.app_version }}'
      helm_chart_name: 'yjb-platform-api'
      helm_dir: 'helm_deploy_api'
      k8s_deployment_name: 'yjb-platform-api'
      slack_notification: 'false'

  # deploy_preprod:
  #   name: Deploy to pre-production environment
  #   needs:
  #     - build
  #     - deploy_dev
  #   uses: ministryofjustice/hmpps-github-actions/.github/workflows/deploy_env.yml@v2 # WORKFLOW_VERSION
  #   secrets: inherit
  #   with:
  #     environment: 'preprod'
  #     app_version: '${{ needs.build.outputs.app_version }}'
  #     slack_notification: 'false' # if 'true', it needs a valid NONPROD_RELEASES_SLACK_CHANNEL variable to be set

  # deploy_prod:
  #   name: Deploy to production environment
  #   needs:
  #     - build
  #     - deploy_preprod
  #   uses: ministryofjustice/hmpps-github-actions/.github/workflows/deploy_env.yml@v2 # WORKFLOW_VERSION
  #   secrets: inherit
  #   with:
  #     environment: 'prod'
  #     app_version: '${{ needs.build.outputs.app_version }}'
  #     slack_notification: 'false' # if 'true', it needs a valid PROD_RELEASES_SLACK_CHANNEL variable to be set
